<UserControl x:Class="WinManager.Views.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{StaticResource PrimaryDarkBrush}">
        <StackPanel VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    MinWidth="320">

            <TextBlock Text="WinManager"
                       FontSize="64"
                       FontWeight="Bold"
                       Foreground="{StaticResource AccentBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,8" />

            <TextBlock Text="Your Windows configuration manager"
                       FontSize="16"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,48" />

            <Button Content="Get Started"
                    Command="{Binding GetStartedCommand}"
                    FontSize="15"
                    FontWeight="SemiBold"
                    Padding="0,14"
                    Margin="0,0,0,12"
                    HorizontalAlignment="Stretch" />

            <Button Content="Load Config"
                    Command="{Binding LoadConfigCommand}"
                    FontSize="15"
                    Padding="0,14"
                    HorizontalAlignment="Stretch"
                    Background="{StaticResource SurfaceBrush}"
                    BorderBrush="{StaticResource BorderBrushMono}"
                    BorderThickness="1" />

            <TextBlock Text="{Binding ConfigStatus}"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       FontSize="12"
                       HorizontalAlignment="Center"
                       Margin="0,16,0,0"
                       Visibility="{Binding ConfigStatus,
                           Converter={StaticResource StringToVisibilityConverter}}" />
        </StackPanel>
    </Grid>
</UserControl>
